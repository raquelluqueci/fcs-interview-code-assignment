apiVersion: 1

providers:
  - name: FCS
    orgId: 1
    folder: FCS Interview
    folderUid: fcs-interview
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
